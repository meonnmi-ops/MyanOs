# MyanOS Web OS v5.1.0 — Requirements
# Core: Python 3.8+ stdlib (no pip needed for basic operation)

# System monitoring (CPU, RAM, Disk, GPU, Network stats)
psutil>=5.9.0

# Database (TiDB Cloud / MySQL compatible)
pymysql>=1.1.0

# AI/LLM (local Ollama integration)
ollama>=0.4.0
